let: x = "Hello, World!"
let: y = x
println(x)

let: i = 0
fornumb(len(x), "i"): {
  x = repchar(x, i, "_")
  
  println(x)
}

fornumb(len(x), ""): {
  i--
  x = repchar(x, i, getchar(y, i))
  
  println(x)
}
